name: Build and Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual triggering

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run linter
        run: npm run lint
        continue-on-error: true # Don't fail build on lint errors
      
      - name: Build project
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL || 'http://localhost:8081' }}
          GITHUB_PAGES: 'true'
          GITHUB_REPOSITORY: ${{ github.repository }}
          CI: 'true'
      
      - name: Verify build output
        run: |
          echo "Checking dist folder contents..."
          ls -la dist/
          echo "Checking index.html for base path..."
          cat dist/index.html
          echo ""
          echo "Checking if CSS files exist..."
          ls -la dist/assets/*.css 2>/dev/null || echo "No CSS files found in assets"
          echo "Checking if JS files exist..."
          ls -la dist/assets/*.js 2>/dev/null || echo "No JS files found in assets"
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

